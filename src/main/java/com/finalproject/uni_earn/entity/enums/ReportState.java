package com.finalproject.uni_earn.entity.enums;

//[ReportState](ReportState) is an enum class that represents the state of a report.
public enum ReportState {
    PENDING,
    RESOLVED,
    REJECTED
}
